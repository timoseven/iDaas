# iDaas 运行配置样例
# 复制为 .env 后按需修改。注意：程序从进程环境变量读取，不会自动解析 .env 文件，
# 启动前请先导入：  set -a; . ./.env; set +a
# 或在 systemd 中使用 EnvironmentFile=.env

# 监听地址与数据库（bbolt 单文件 .db 二进制存储）
LISTEN_ADDR=:8088
DB_PATH=idaas.db

# 会话相关密钥（生产环境必须改为强随机值，例如：openssl rand -hex 32）
SECRET_KEY=change-me-to-a-strong-random-secret

# --- SAML 2.0 IdP ---
# EntityID，建议即对外可访问的 metadata URL
SAML_ENTITY_ID=http://localhost:8088/saml/metadata
# 站点对外 base URL（用于 metadata 中的 SSO endpoint Location）
SAML_BASE_URL=http://localhost:8088
# 阿里云固定 ACS，勿改
SAML_ACS_URL=https://signin.aliyun.com/saml/SSO
# IdP 证书与私钥路径
SAML_CERT_PATH=certs/idp.crt
SAML_KEY_PATH=certs/idp.key
# 阿里云侧创建 SAML IdP 后回填：acs:ram::<主账号ID>:saml-provider/<IdP名>
# 详见 docs/aliyun-setup.md；为空时角色控制台登录会报错
SAML_IDP_ARN=
# Assertion 有效期（分钟）
SAML_ASSERTION_VALID_MINUTES=5
# metadata 中的组织信息（可选）
SAML_ORG_NAME=iDaas
SAML_ORG_DISPLAY_NAME=iDaas Identity Provider
SAML_ORG_URL=
SAML_CONTACT_EMAIL=
